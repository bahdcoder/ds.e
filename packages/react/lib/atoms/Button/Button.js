import React$1 from 'react';

const Button = ({ label }) => {
    return React$1.createElement("button", { className: 'dse-button-container' },
        label || 'Button',
        " - LABEL HERE");
};

export default Button;
//# sourceMappingURL=Button.js.map
